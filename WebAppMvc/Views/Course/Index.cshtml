@model  CleanWebApp.Application.ViewModels.CourseViewModel
<div class="container body-content" >
    <div class="row course-content">
@foreach (var Course in Model.Courses)
{
    <div class="course-image-container">
        <div class="course-image" style="background-image:url(@Course.Imge)">

        </div>
    </div>
    <span>
        @Course.Title
    </span>
}
    </div>
    </div>>